import 'package:flutter/material.dart';

class ADTabbar extends StatelessWidget {
  const ADTabbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
